{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "scripts": {
    "scripts:dev": "vitepress dev config",
    "scripts:build": "vitepress build config",
    "scripts:preview": "vitepress preview config"
  }
}