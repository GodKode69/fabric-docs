{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "scripts": {
    "docs:dev": "vitepress dev md --config config/.vitepress",
    "docs:build": "vitepress build md --config config/.vitepress",
    "docs:preview": "vitepress preview md --config config/.vitepress"
  }
}